export default class Message {
        _id: string;
     userId: string;
       text: string;
  createdAt: string;

  constructor() {
    this._id       = '';
    this.userId    = '';
    this.text      = '';
    this.createdAt = '';
  }
}
